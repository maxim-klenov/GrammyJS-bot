async function sendWelcomeMessage(ctx) {
  // Продолжить выполнение кода
}

async function sendRestartMessage(ctx) {
  // Продолжить выполнение кода
}

module.exports = { sendWelcomeMessage, sendRestartMessage };